TASK: Add a pro “Reset Peaks” control (UI-only wiring)

Scope (ONLY):
- Source/ui/layout/HeaderBar.h
- Source/ui/layout/HeaderBar.cpp
- Source/ui/MainView.h
- Source/ui/MainView.cpp
- Source/ui/analyzer/AnalyzerDisplayView.h
- Source/ui/analyzer/AnalyzerDisplayView.cpp

Rules:
- Do NOT modify AnalyzerEngine DSP logic (assume AnalyzerEngine::resetPeaks() exists).
- UI-only: call resetPeaks() from the message thread.
- No allocations in paint(); no locks.
- Minimal diff; do not redesign layout.

Goal:
- Icon-only reset button in HeaderBar.
- Keyboard shortcut triggers the same action.
- Subtle visual feedback (brief flash) after reset.

Requirements:
1) HeaderBar button:
   - Icon glyph (unicode) with no label text.
   - Tooltip describing shortcut.
   - Fixed square size so it doesn’t clip on Retina.
2) Shortcut:
   - Must not fire when typing in a TextEditor.
3) Feedback:
   - Flash peak trace for ~150ms (UI-only).

Deliverable:
- Build passes; click + shortcut both reset peaks and show a subtle flash.

