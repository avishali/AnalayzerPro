Mission 2 — Faster + smoother fall to -inf when signal stops, and Release Time affects all traces

Scope: unify decay/hold logic so “Release Time” is the single knob affecting FFT trace + other traces (RMS/L/R/M/S if present) + meter falloff.

IMPLEMENTER PROMPT — Mission 2

Return only the final code changes. No markdown.

Goal:
	1.	When signal stops, FFT trace + meters currently fall to -inf too slowly. Make the decay faster but still smooth (no step/jitter).
	2.	The existing “Release Time” control must affect all traces (not only Peak).

Rules:
	•	Do NOT change the Peak algorithm (it’s its own thing), but its display must remain consistent.
	•	Implement a single “ballistics/decay” function used by:
	•	FFT display trace fall/decay to floor
	•	All non-peak traces (RMS/Avg/whatever) fall/decay
	•	Meter falloff (if currently separate)
	•	Make decay time constant derived from Release Time parameter.
	•	Smoothness: use a one-pole envelope style or exponential smoothing with a coefficient derived from dt and release time.
	•	When input magnitude is effectively zero (or below a noise gate threshold already used), decay should continue toward floor quickly.

Engineering details:
	•	Must be sample-rate/block-rate safe: if updates are per GUI timer, use dt from timer interval; if updates are per audio block, use block duration.
	•	No allocations in paint().
	•	Ensure the trace does not “stick” when silence.
	•	Keep the UI and parameter list unchanged (no new params in this mission).

Definition of Done:
	•	On signal stop, FFT and meters reach floor “fast but not abrupt” (audibly like a pro analyzer).
	•	Release Time knob clearly changes how long it takes to decay for every non-peak trace + meters.

VERIFIER PROMPT — Mission 2

Return only pass/fail checklist + failing notes.

Verify in a host:
	•	Play steady tone → stop transport or mute track.
	•	Observe: FFT trace decays smoothly and faster than before.
	•	Observe: meters decay smoothly and faster than before.
	•	Turn Release Time: all non-peak traces and meters change decay rate.
	•	No “stepping squares”, no sudden jumps, no flicker.
	•	No CPU spikes.

STOP. You test.
