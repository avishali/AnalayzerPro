Return only the final content.
No markdown. No explanation.

Task: Add a CMake option in AnalyzerPro to choose mdsp_ui source:
	•	Default: use vendored third_party/melechdsp-hq (current behavior).
	•	Dev override: allow using a local HQ repo path via -DMELECHDSP_HQ_ROOT=/abs/path/to/melechdsp-hq

Requirements:
	•	If MELECHDSP_HQ_ROOT is set and exists, use it.
	•	Otherwise fall back to third_party/melechdsp-hq.
	•	Emit a CMake STATUS message showing which path is used.
	•	Do NOT change target names or library linkage beyond swapping the add_subdirectory source path.
	•	Keep build behavior identical when using third_party.

Implementation:
	1.	In AnalyzerPro top-level CMakeLists.txt (or the place where you currently add third_party/melechdsp-hq), introduce:
	•	option(MELECHDSP_USE_VENDORED_HQ “…” ON) OR simply a cache PATH variable MELECHDSP_HQ_ROOT.
Prefer: set(MELECHDSP_HQ_ROOT “” CACHE PATH “Path to melechdsp-hq; if empty, use third_party copy”)
	2.	Compute HQ_DIR:
	•	if (MELECHDSP_HQ_ROOT AND EXISTS “${MELECHDSP_HQ_ROOT}/shared/mdsp_ui/CMakeLists.txt”) use that
	•	else use “${CMAKE_CURRENT_SOURCE_DIR}/third_party/melechdsp-hq”
	3.	Replace the existing add_subdirectory(third_party/melechdsp-hq …) with add_subdirectory(”${HQ_DIR}” …)
	4.	Add:
	•	message(STATUS “AnalyzerPro: using melechdsp-hq from: ${HQ_DIR}”)

Output:
Return ONLY the final content of the modified AnalyzerPro CMakeLists.txt file(s).

How you’ll use it locally:
	•	Configure with:
cmake -S . -B build -DMELECHDSP_HQ_ROOT=/Users/avishaylidani/DEV/GitHubRepo/MelechDSP/melechdsp-hq
	•	Or leave it empty to keep vendored behavior.
