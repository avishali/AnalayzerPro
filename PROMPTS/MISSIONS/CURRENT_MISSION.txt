Return only the final prompt content.
No markdown.
No explanation.

You are Cursor inside the AnalyzerPro JUCE project.

GOAL
Harden the new top-bar pixel-snapping + inside-stroke implementation against edge cases:
	•	Prevent negative corner radii after inside-stroke adjustment
	•	Ensure fill/stroke rects share the exact same snapped basis (no drift)
	•	Ensure row layout snapping does not accumulate rounding error across multiple buttons

HARD REQUIREMENTS
	•	No visual redesign; keep the same look.
	•	No layout changes except micro-adjustments needed for deterministic snapping.
	•	No DSP/audio changes.
	•	macOS warning-clean.

TASKS
	1.	Clamp stroke radius safely
In HeaderBarLookAndFeel (or wherever strokeRadius is computed):

	•	If strokeRadius = radius - 0.5f, clamp:
strokeRadius = juce::jmax(0.0f, radius - 0.5f);
	•	If any snapRadius helper exists, ensure the clamped value is snapped after clamp (or snap then clamp, but be consistent).

	2.	Derive inside-stroke rect from snapped fill rect

	•	Ensure fill rect is snapped once:
fillR = PixelSnap::snapRectToPixels(bounds.toFloat())
	•	Compute stroke rect only from fillR:
strokeR = PixelSnap::insetForInsideStroke(fillR, 1.0f)
	•	Do not recompute from original bounds.

	3.	Prevent cumulative rounding drift in button row layout
In HeaderBar::resized():

	•	Ensure canonical row is snapped once (int coords).
	•	Ensure buttonH and y are integer pixels.
	•	When placing buttons left→right:
	•	snap x once at start
	•	snap each width
	•	gap is integer
	•	next x = x + w + gap using integer arithmetic
	•	Ensure last button stays within row (clamp width if needed by 1px rather than overflowing).

DELIVERABLES
	•	FILES CHANGED
	•	SUMMARY of the hardening changes
	•	Unified diffs
