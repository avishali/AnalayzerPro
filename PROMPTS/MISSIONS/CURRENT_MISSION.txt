Return only the final content of PEAK_HOLD_UNIFY_FIX.md.
No markdown. No explanation.

Goal: Unify Peak Hold behavior after HQ migration:
1) Peak trace remains visible after stopping signal when Peak Hold is ON.
2) Hover label in Peak Hold shows peak-hold value (decimated/unclamped) and does not track any “invisible” live trace.
3) Global max marker uses same peak-hold buffer/domain as peak trace.

Constraints:
- Minimal patch, no per-frame allocations.
- Single source of truth: HQ RTADisplayModel caches + renderer reads those caches.
- AnalyzerPro should not keep its own peak-hold buffers if HQ provides them.

Tasks:

A) Locate and remove/disable legacy hover label source when Peak Hold is enabled
1) Search for any label drawing that uses hoverDbCrosshair / getDbAtPixelX / controller hoverDb.
   rg -n "hoverDbCrosshair|getDbAtPixelX|formatDb\\(|HoverDb|dbCrosshair" shared/mdsp_ui AnalyzerPro
2) Ensure there is exactly ONE FFT hover label draw path in HQ renderer.
3) In that path:
   - If Peak Hold enabled:
       dbLabel := model.getDecimatedPeakAtX(xPlotPx, yPlotPx, &dbUnclamped) ? dbUnclamped : (fallback peakDbTraceAtBin)
   - Else:
       dbLabel := hoverDbCrosshair (or live decimated)
   - Print source in debug: "labelSrc=HOLD|LIVE|FALLBACK"

B) Fix peak trace cache clearing on silence
1) In RTADisplayModel::buildFftPaths (or equivalent):
   - Identify logic that clears cachedPeakPath_ / cachedPeakDbUnclamped_ when hasPeakSignal is false.
2) Change it so:
   - If Peak Hold is enabled AND there is a valid cached peak from previous frames:
       do NOT clear caches when current signal is silent.
   - Only clear on:
       - explicit Reset Peaks action
       - FFT size/config mismatch
       - model invalidation
   - Add a small boolean cachedPeakValid_ that stays true once built until reset/invalidation.

C) Fix global max marker domain
1) Find where the “yellow small mark” is computed/drawn.
2) When Peak Hold enabled:
   - Use cachedPeakDbUnclamped_ (or compensated equivalent if you decide) to find max.
   - Use cachedPeakX/Y plot arrays to place marker exactly on the cached peak curve.
3) Ensure marker doesn’t disappear on silence as long as cachedPeakValid_ is true.

D) DoD
- With Peak Hold ON:
  - Play audio -> peak visible.
  - Stop audio -> peak remains visible (held).
  - Hover -> label shows held peak db, not live tracking.
  - Global max marker is on the held peak, not 20 dB below.
- With Peak Hold OFF:
  - Behavior unchanged from current.

Deliver:
- exact files/functions edited
- brief reasoning per change
- remove debug logs or keep behind MDSP_DEBUG_CROSSHAIR