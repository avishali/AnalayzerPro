Paste-ready Implementer prompt (Cursor)

Return only the final modified content for the touched files. No markdown. No explanation.

Goal:
	•	Option A stays: Gaussian smoothing happens ONLY in UI log domain (256 log bins) in LINEAR POWER.
	•	Engine never performs spectral smoothing when Option A is active.
	•	“engineDidSpectralSmooth” flag is correctly produced by Engine and consumed by UI (single source of truth).
	•	Remove any possibility of double-smoothing (Engine + UI, or UI SmoothingProcessor + UI Gaussian).
	•	Remove ALL heap allocations from the audio thread (multi-trace included).

Tasks:
	1.	AnalyzerEngine.cpp
	•	Remove / disable the prefix-sum spectral smoothing path cleanly (do not leave a hardcoded engineWillSmooth=false inside the loop).
	•	Add a clear config point: spectralSmoothingStage = UISmoothingLogGaussian (or similar) and set snapshot.engineDidSpectralSmooth = false when Option A is used.
	•	Fix multi-trace: eliminate std::vector allocations in the audio thread. Preallocate member buffers sized to numBins in prepareToPlay / on FFT size change.
	2.	AnalyzerSnapshot (where defined)
	•	Ensure it has bool engineDidSpectralSmooth = false; (or equivalent) and it is set every publish.
	3.	AnalyzerDisplayView.cpp
	•	In convertFFTToLog:
	•	Build logPower[256] in linear power (already done via db->power conversion).
	•	Apply LogGaussianSmoother ONLY when octaves > 0 AND snapshot.engineDidSpectralSmooth == false.
	•	After smoothing, convert smoothedPower -> dB for logDb.
	•	Ensure UI SmoothingProcessor is not applied on the same data path (either remove from FFT path or force bypass always for FFT mode when Option A is in use).

Constraints:
	•	Real-time safe: no allocations or resizes in audio processing callbacks.
	•	UI smoothing config (1/24..1 Oct) maps to gaussian sigma in log-bin space consistently.
	•	Peak trace remains independent.

Files likely touched:
	•	Source/analyzer/AnalyzerEngine.cpp
	•	Source/analyzer/AnalyzerSnapshot.h (or wherever AnalyzerSnapshot lives)
	•	Source/ui/analyzer/AnalyzerDisplayView.cpp

⸻
