Implementer patch (paste-ready)

Put this after:
	•	juce_add_plugin(${PLUGIN_NAME} ...) is called
	•	and after add_subdirectory("${HQ_DIR}" ...) (so mdsp targets exist)

	# ------------------------------------------------------------------
# Ensure mdsp SDK include dirs/defs propagate to the actual JUCE build targets
# ------------------------------------------------------------------
set(_mdsp_consumer_targets
    ${PLUGIN_NAME}
    ${PLUGIN_NAME}_Standalone
    ${PLUGIN_NAME}_VST3
    ${PLUGIN_NAME}_AAX
    ${PLUGIN_NAME}_SharedCode
)

foreach(t IN LISTS _mdsp_consumer_targets)
    if (TARGET ${t})
        if (TARGET mdsp::sdk)
            target_link_libraries(${t} PRIVATE mdsp::sdk)
        elseif (TARGET mdsp_sdk)
            target_link_libraries(${t} PRIVATE mdsp_sdk)
        else()
            message(FATAL_ERROR "mdsp SDK target not found (expected mdsp::sdk or mdsp_sdk). Check HQ_DIR/add_subdirectory.")
        endif()
    endif()
endforeach()


And remove the earlier hard FATAL_ERROR that expects TARGET AnalyzerPro.

This way:
	•	You won’t fail if the compile target isn’t exactly AnalyzerPro.
	•	You’ll still attach mdsp to the correct build targets that actually compile your sources.

⸻

Also: your HQ_DIR log shows a potential path issue

You print:
AnalyzerPro: using melechdsp-hq from: .../third_party/melechdsp-hq

But earlier your submodule folder listing showed:
third_party/melechdsp-hq/melechdsp-hq/... (nested)

Right now your log also shows it found:
third_party/melechdsp-hq/shared/mdsp_ui/CMakeLists.txt
So in this checkout, shared/ is directly under third_party/melechdsp-hq/, which is fine.

So don’t change HQ_DIR unless you see it missing.

