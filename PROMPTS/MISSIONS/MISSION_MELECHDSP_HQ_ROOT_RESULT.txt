PLAN + FILES

1. Add MELECHDSP_HQ_ROOT cache var near top-level (after project() or after JUCE path block): set(MELECHDSP_HQ_ROOT "" CACHE PATH "Path to main melechdsp-hq repo root (must contain shared/mdsp_ui/CMakeLists.txt)"). If empty, get_filename_component(_default_hq "${CMAKE_CURRENT_SOURCE_DIR}/../melechdsp-hq" ABSOLUTE) then set(MELECHDSP_HQ_ROOT "${_default_hq}" CACHE PATH "..." FORCE). get_filename_component(MELECHDSP_HQ_ROOT ... ABSOLUTE). Validate: if(NOT EXISTS "${MELECHDSP_HQ_ROOT}/shared/mdsp_ui/CMakeLists.txt") message(FATAL_ERROR ...). message(STATUS "MELECHDSP_HQ_ROOT: ${MELECHDSP_HQ_ROOT}").

2. Replace add_subdirectory(third_party/melechdsp-hq/shared/mdsp_*) with add_subdirectory("${MELECHDSP_HQ_ROOT}/shared/mdsp_core" "${CMAKE_CURRENT_BINARY_DIR}/hq_mdsp_core") and same for mdsp_ui, mdsp_dsp, mdsp_gui with explicit binary dirs.

3. Replace include dirs ${CMAKE_CURRENT_SOURCE_DIR}/third_party/melechdsp-hq/shared/mdsp_ui/include and mdsp_gui/include with ${MELECHDSP_HQ_ROOT}/shared/mdsp_ui/include and ${MELECHDSP_HQ_ROOT}/shared/mdsp_gui/include.

4. Leave target_link_libraries(… mdsp_ui mdsp_dsp mdsp_core mdsp_gui …) unchanged.

FILE IN SCOPE: AnalyzerPro/CMakeLists.txt

PATCH

--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -65,6 +65,22 @@ if(NOT EXISTS "${JUCE_PATH}")
     message(FATAL_ERROR "JUCE path not found: ${JUCE_PATH}\nPlease set JUCE_PATH env var or -DJUCE_PATH=/path/to/JUCE")
 endif()
 
+# ==============================================================================
+# MelechDSP HQ root (main repo path; overrides third_party copy)
+# ==============================================================================
+
+set(MELECHDSP_HQ_ROOT "" CACHE PATH "Path to main melechdsp-hq repo root (must contain shared/mdsp_ui/CMakeLists.txt)")
+if(MELECHDSP_HQ_ROOT STREQUAL "")
+    get_filename_component(_default_hq "${CMAKE_CURRENT_SOURCE_DIR}/../melechdsp-hq" ABSOLUTE)
+    set(MELECHDSP_HQ_ROOT "${_default_hq}" CACHE PATH "Path to main melechdsp-hq repo root (must contain shared/mdsp_ui/CMakeLists.txt)" FORCE)
+endif()
+get_filename_component(MELECHDSP_HQ_ROOT "${MELECHDSP_HQ_ROOT}" ABSOLUTE)
+message(STATUS "MELECHDSP_HQ_ROOT: ${MELECHDSP_HQ_ROOT}")
+if(NOT EXISTS "${MELECHDSP_HQ_ROOT}/shared/mdsp_ui/CMakeLists.txt")
+    message(FATAL_ERROR "Invalid MELECHDSP_HQ_ROOT: ${MELECHDSP_HQ_ROOT} (expected shared/mdsp_ui/CMakeLists.txt)")
+endif()
+
 # ==============================================================================
 # PROJECT SETUP
 # ==============================================================================
@@ -118,14 +134,10 @@ add_subdirectory("${JUCE_PATH}" "${CMAKE_CURRENT_BINARY_DIR}/JUCE" EXCLUDE_FROM_
 # MelechDSP shared modules (from MELECHDSP_HQ_ROOT)
 # ==============================================================================
 
-add_subdirectory(third_party/melechdsp-hq/shared/mdsp_core)
-add_subdirectory(third_party/melechdsp-hq/shared/mdsp_ui)
-add_subdirectory(third_party/melechdsp-hq/shared/mdsp_dsp)
-add_subdirectory(third_party/melechdsp-hq/shared/mdsp_gui)
+add_subdirectory("${MELECHDSP_HQ_ROOT}/shared/mdsp_core" "${CMAKE_CURRENT_BINARY_DIR}/hq_mdsp_core")
+add_subdirectory("${MELECHDSP_HQ_ROOT}/shared/mdsp_ui"   "${CMAKE_CURRENT_BINARY_DIR}/hq_mdsp_ui")
+add_subdirectory("${MELECHDSP_HQ_ROOT}/shared/mdsp_dsp"  "${CMAKE_CURRENT_BINARY_DIR}/hq_mdsp_dsp")
+add_subdirectory("${MELECHDSP_HQ_ROOT}/shared/mdsp_gui"  "${CMAKE_CURRENT_BINARY_DIR}/hq_mdsp_gui")
 
 # ==============================================================================
 # PLUGIN TARGET
 # ==============================================================================
@@ -228,8 +240,8 @@ target_compile_features(${PLUGIN_NAME} PUBLIC cxx_std_17)
 target_include_directories(${PLUGIN_NAME}
     PRIVATE
         Source
-        ${CMAKE_CURRENT_SOURCE_DIR}/third_party/melechdsp-hq/shared/mdsp_ui/include
-        ${CMAKE_CURRENT_SOURCE_DIR}/third_party/melechdsp-hq/shared/mdsp_gui/include
+        ${MELECHDSP_HQ_ROOT}/shared/mdsp_ui/include
+        ${MELECHDSP_HQ_ROOT}/shared/mdsp_gui/include
 )
 
 target_compile_definitions(${PLUGIN_NAME}
