TASK: Add user-selectable analyzer display dB range (-60 / -90 / -120)

Scope (ONLY):
- Source/ui/analyzer/AnalyzerDisplayView.h
- Source/ui/analyzer/AnalyzerDisplayView.cpp
- Source/ui/layout/HeaderBar.h
- Source/ui/layout/HeaderBar.cpp
- Source/ui/MainView.h
- Source/ui/MainView.cpp

Rules:
- UI-only: do NOT touch AnalyzerEngine or FFT math.
- Grid + FFT + Peak must use the same display min/max dB.
- Minimal diff; no class rewrites; no new architecture.

Goal:
- User can select -60 / -90 / -120 dB range.
- Default is -120 dB.
- Switching updates the plot immediately (grid and traces).

Implementation (high-level):
1) AnalyzerDisplayView:
   - Add enum DbRange and state.
   - Map DbRange → minDb.
   - Apply (maxDb=0, minDb) consistently for grid + y-mapping.
2) HeaderBar:
   - Add a “dB Range” ComboBox with items -60/-90/-120.
   - Wire selection to AnalyzerDisplayView::setDbRange(...).
3) MainView:
   - Connect HeaderBar callback to the view.

Deliverable:
- Build passes; range changes scale grid + FFT + peak consistently.

