Return only the final prompt content.
No markdown.
No explanation.

You are Cursor inside the AnalyzerPro JUCE project.

GOAL
Lock in the “no double drawing” button rendering architecture and prevent regressions:
	•	Ensure no LookAndFeel function draws text twice (TextButton and ToggleButton).
	•	Ensure icon-only buttons never have visible text (must be setButtonText(””)).
	•	Ensure SVG icons are not drawn twice via state images (normal/over/down) layering.
	•	Add lightweight debug assertions (JUCE_DEBUG only) to catch violations early.

HARD REQUIREMENTS
	•	No layout changes.
	•	No DSP/audio changes.
	•	Must build warning-clean on macOS.
	•	Debug checks must compile out in release.

TASKS
	1.	LookAndFeel: enforce single-text-path invariant

	•	Search for any overrides or calls that might double-render text:
	•	drawTextButton
	•	drawButtonText
	•	drawToggleButton
	•	LookAndFeel_V4::drawToggleButton
	•	LookAndFeel_V4::drawButtonText
	•	Ensure:
	•	TextButton: only drawButtonText draws the text (and it returns early if text empty).
	•	ToggleButton: only drawToggleButton draws the toggle text once (and returns early if text empty).
	•	If any base calls would draw text, remove them or replace with background-only helper.

	2.	Icon-only buttons: enforce empty text at construction

	•	Find all buttons intended to be icon-only (top bar buttons, collapsible arrows, small utility buttons).
	•	Ensure each such button explicitly calls setButtonText(””).
	•	If any icon-only buttons currently rely on text for tooltip/accessibility:
	•	Keep tooltip/accessibility name, but button text must remain empty.

	3.	SVG state images: prevent double icon compositing

	•	Inspect SvgIconButton / DrawableButton usage (setImages / setImage / setImages with multiple drawables).
	•	Confirm we are not accidentally stacking multiple Drawables at the same time:
	•	Do not set both “normal image” and also paint an icon manually.
	•	For state images, ensure only one drawable is used per state (normal OR over OR down), not “normal + over simultaneously”.
	•	If the current helper uses the same Drawable pointer mutated/tinted for multiple states, fix it so each state uses its own immutable Drawable instance (no shared mutation that could render twice).

	4.	Add JUCE_DEBUG-only regression checks

	•	For TextButton: in drawButtonText(), add a debug check that paintTextButton did not draw text.
	•	Implement as a simple global/thread-local “currentlyDrawingButtonText” guard:
	•	Set flag at start of drawButtonText and clear at end.
	•	In ButtonPaint.cpp (paintTextButton / paintToggleButton), assert that the flag is false and that these functions never call drawText/drawFittedText (they shouldn’t exist anymore, but guard anyway).
	•	For icon-only buttons: in their setup code (where icons are assigned), DBG()/jassert that getButtonText().isEmpty().

	5.	Verification

	•	Confirm ripgrep shows no drawText/drawFittedText in ButtonPaint.cpp.
	•	Confirm no LookAndFeel base calls that draw text remain.
	•	Confirm icon-only buttons setButtonText(””).
	•	Confirm no doubled icons/text in UI.

DELIVERABLES
	•	FILES CHANGED
	•	SUMMARY of what invariants are enforced and where
	•	Unified diffs for all changed files