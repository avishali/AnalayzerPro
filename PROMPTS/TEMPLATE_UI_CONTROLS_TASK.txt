TASK: <Short, explicit UI control task>

Subsystem:
- HeaderBar / Rail / MainView / Editor

Scope:
- <Exact file list>

Rules:
- UI wiring only.
- Engine calls via existing public API only.
- No DSP logic here.

Context:
<Describe the user interaction and UX intent.>

Steps:
1) <Add control / button / combo>
2) <Wire callback>
3) <Keyboard shortcut (if applicable)>

UX Requirements:
- Discoverable but not intrusive.
- No modal dialogs unless explicitly required.

Do NOT:
- Duplicate state already owned by APVTS.
- Add magic numbers for layout without comment.

Acceptance:
- Control works as described.
- No layout breakage at minimum window size.
- No conflicts with text input focus.
